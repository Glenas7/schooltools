import React from 'react';
import SchedulerLoginForm from '../components/auth/SchedulerLoginForm';

const SchedulerLogin: React.FC = () => {
  return <SchedulerLoginForm />;
};

export default SchedulerLogin;